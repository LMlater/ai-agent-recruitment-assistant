# Data

This directory is reserved for public or simulated data used in later iterations.

- `raw/`: original public datasets or generated mock samples.
- `processed/`: cleaned and mapped datasets.
- `eval/`: RAG and multi-agent review evaluation sets.

The first round uses seed SQL and mock policy documents only. Do not store real bank customer data, real identity card numbers, real phone numbers, or secrets here.
